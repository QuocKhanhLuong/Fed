# Federated Learning with QUIC Protocol and LoRA
# Core dependencies for FL-QUIC-LoRA research project

# QUIC Protocol
aioquic>=0.9.21
cryptography>=41.0.0

# Federated Learning Framework
flwr>=1.5.0
flwr-datasets>=0.0.2

# PyTorch and Deep Learning
# NOTE: torch>=2.6.0 required for secure model loading (CVE-2025-32434)
torch>=2.6.0
torchvision>=0.20.0
transformers>=4.30.0
peft>=0.5.0  # LoRA implementation
safetensors>=0.4.0  # Secure model loading alternative
accelerate>=0.25.0  # For efficient model loading

# Compression and Serialization
lz4>=4.3.2
numpy>=1.24.0

# Utilities
tqdm>=4.65.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Logging and Monitoring
tensorboard>=2.13.0
wandb>=0.15.0  # Optional: for experiment tracking

# Development tools
pytest>=7.4.0
black>=23.7.0
flake8>=6.1.0

# For Jetson Nano (ARM64) - install separately if needed
# torch==2.0.0+cu118 (from PyTorch wheel for Jetson)
# torchvision==0.15.0+cu118
